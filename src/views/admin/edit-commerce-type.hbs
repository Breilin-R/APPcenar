<div class="row">
    <div class="col-md-6 mx-auto">
        <div class="card">
            <div class="card-header">
                <h3>Editar Tipo de Comercio</h3>
            </div>
            <div class="card-body">
                <form action="/admin/commerce-types/edit/{{type._id}}" method="POST">
                    <div class="mb-3">
                        <label>Nombre</label>
                        <input type="text" name="name" class="form-control" value="{{type.name}}" required>
                    </div>
                    <div class="mb-3">
                        <label>Descripción</label>
                        <textarea name="description" class="form-control" required>{{type.description}}</textarea>
                    </div>
                    <div class="d-grid gap-2">
                        <button class="btn btn-primary">Actualizar Tipo</button>
                        <a href="/admin/commerce-types" class="btn btn-secondary">Cancelar</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>